<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link for "tap to review" on a captured transaction. A custom
                 scheme rather than an App Link: App Links need a verified
                 assetlinks.json on a web host, which a sideloaded personal app
                 has no use for. Capacitor's Bridge turns a matching ACTION_VIEW
                 intent into the JS `appUrlOpen` event on its own. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="cashflow" android:host="review" />
            </intent-filter>

        </activity>

        <!-- Reads Advanzia's transaction notifications. Bound by the system, not
             by the app, which is what lets it run while Cashflow is dead. The
             permission and intent-filter below are both required for the system
             to recognise it as a notification listener at all. -->
        <service
            android:name=".AdvanziaNotificationListener"
            android:label="@string/app_name"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Guard 1: the four-hour nudge about unconfirmed captures. -->
        <receiver
            android:name=".CaptureNagReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required from Android 13 (targetSdk is 35) or every notification this
         app posts is silently dropped by the OS — including the capture-health
         alarm, which is the one that reports capture being broken. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
